---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Immich
  ansible.builtin.systemd:
    name: immich
    state: restarted
    daemon_reload: true

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
