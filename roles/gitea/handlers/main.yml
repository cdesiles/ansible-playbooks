---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart gitea
  ansible.builtin.systemd:
    name: gitea
    state: restarted
    daemon_reload: true

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
